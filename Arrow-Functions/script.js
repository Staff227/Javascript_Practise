let num1, num2, result;
const add = (num1, num2) => {
   result = num1 * num2;
    return result;
};

    document.write(add(5, 12));


/*     const sayHi = (name) => {
        document.write("<h1>" + "How are u doing " + name + "</h1>")
    }
    
    sayHi("mustapha"); */